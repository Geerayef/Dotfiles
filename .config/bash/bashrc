# ~  Bash

# ~ ! Sourced from /etc/bash.bashrc

settings="$BASHDOTDIR/settings"

# ~  Environment variables
source $settings/env.bash

# ~  PATH
source $settings/path.bash

# ~  Aliases
source $settings/alias.bash

eval "$(starship init bash)"
