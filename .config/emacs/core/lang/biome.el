;;;; biome.el --- LSP-mode integration for Biome -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

;; (use-package lsp-biome
;;   :ensure (:host github :repo "cxa/lsp-biome"))

(provide 'biome)
;; biome.el ends here
